const initialState = {
    position: [0,0],
    spriteLocation: 'center top left'
}

const catReducer = (state=initialState, action) => {
    switch(action.type) {
        default:
            return state
    }
}

export default catReducer;